# api/ — 后端 API 通信层

一旦我所属的文件夹有所变化，请更新我。

fetch 封装 + 类型定义 + KDF 参数映射，是前端与后端的唯一通信通道。

| 文件 | 地位 | 功能 |
|------|------|------|
| client.ts | API 基础设施 | apiFetch 通用请求、Bearer token 注入、错误类型、请求/响应类型、KDF 参数映射 |
